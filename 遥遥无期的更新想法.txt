V3.5.5
移植合并原版新内容，比如hid自适应分辨率，前端，……


V3.6.0
上下车键（配置热切换切换键）（热更新）创建新的文件夹如key_k会识别文件名中是否含有如KEY_K（手动创建复制然后重命名），按下点击位置切换到配置并打开映射，智能的配置切换键能知道切回启动时的配置文件，在KEY_K配置文件里可以切回主配置文件 N
提示当前配置文件
可以以当前配置文件复制并重命名出来
各种提示容错如找不到配置切换键文件
会创建配置文件如key_k.json（以启动的配置文件复制重命名）N
一整套紧密逻辑，互不干扰
如果要修改只能重新启动配置文件进行修改，可以切换配置文件后刷新网页，不需要重新启动程序，
会克隆出来，有当前文件名称，不可更改
如果文件不存在，更新配置时会从主配置文件克隆复制重命名为标题
还要向前端分享启动时的文件名（配置文件作为通信）
草稿
需要更好的设计
config_manager.go
把mian.go中的配置文件管理转移到  ？
仅可以在开启映射时触发，或者自定义
可以和宏事件联动
配置文件作为通信告知主配置文件子配置文件
启动时的文件作为主配置文件，这里要整理清楚切换配置文件后，子配置文件
子配置文件在前端可输入文件名（不带json），可以备注。
想像宏一样可以添加卡片，
宏联动可以以文件名，或卡片数字代号触发
是否在切换配置时不动开关映射？为了无缝
处理前端tab满了，确认为滚动滑动逻辑
前端可以显示当前文件名，可能需要传输接口，可以重命名文件，
游戏的载具上车操作是无缝的，所以在热切换配置时不能释放轮盘触点，视角触点
但保留更新配置时关闭映射与重载，不不在前端加个自动更新配置的功能，来自前端的配置更新不会重载，也就是需要开启配置重载才可以
前端显示当前配置文件，同时打印当前配置文件，逻辑为切换配置时后端把文件名送到前端更新一次，同时前端重新加载配置

切换配置时重置一次视角  开关，或者释放所有触点重置？也就是更新配置时重载选项开关
既然这个专门用来处理配置的，那么在前端可以加个，导入导出配置

v3.6.5 宏v2
v3.7.0 组合键路由
v3.7.5 虚拟光标更多功能 优化滚轮惯性
v3.8.0 滚轮滑块升级 优化惯性
v3.9.0 压枪升级
v4.0.0 调整功能，如启用轮盘，和其他剩余杂项优化，功能，视角键自定义去程速度半径厚度，丝滑墙壁

v3.00（不发布）
整理文件，想整理前端组件文件，handler模块与main.go的联系。 不打印日志 而转为debug显示；想整理前端组建文件，handler模块与main.go的联系。各种关联逻辑链路谁与谁处理的细分。
甚至想二次模块化handler_keys，分成handler_keytype
调整按键功能类型的名称命名，更优雅的名称。
————————————————————
v x.x.x可能做 ————————
滚轮滑块能独立映射触点，不与上下滚轮抢触点，比如开启滚轮滑块加有上下滚轮的映射位置，滚动滚动滚轮就能触发滚轮滑块和触点位置两处映射，滚轮滑块仅监听上下滚轮是否滚动，不会影响滚轮键功能样式设定的位置，
滚轮滑块新模式 板块里可绑定键功能是，:按下启用滚轮滑块，抬起关闭（监听），每个绑定键都可以选择位置（可视化样式为一个新的滚轮滑块但有键字体提示），这个位置就是滚轮滑块触发位置，逻辑是按下键然后滚动滚轮就会在这个键位置触发滚轮滑动映射触点，用于绑定右键开启倍镜滚动调整倍镜，或开启地图时滚动缩放……Y
滚轮滑块 的放大模式，上滚放大，下滚缩小，可角度调整。
为了还原端游操作，这里没有滚轮滑块切出时，可以用上下滚轮映射点击切枪，
可以设定重置位移键，按下键后启用滑块并在 可设定的位置切出，常用:右键 背包键。可设定关闭滑块键按下就关闭 常用:1234键。就可以开镜滚轮 或者地图 背包滚动 ，按123切枪后又可以滚轮切枪。
要宏化滚轮滑块，有两个滑块，倍镜滑块，地图缩放滑块，

滚轮滑块与切出指针时的状态无关

高级轮盘可以设置8方向位置的绑定按键，N
有第三个圈的半径位置可以绑定键，N
轮盘有边界，碰到屏幕边缘有限制，Y
轮盘的随机动态最小半径？

连点加副本位置，可以有多个位置（宏） N
+
小眼睛（视角重置键）可设置鼠标速度的键功能样式。。可以单独设置随机重置位置的半径。。可以单独启用半径。可以单独调节重置归中半径。v3.5.0有个问题:启用重置半径，滑动小眼睛 （PUBG看屁股，瓦技能）有可能会限制滑动， 还有圆形的区域会让鼠标无法移动，把圆形转为矩形？

创建伪装的虚拟触摸屏（定制改名vidpid等等） N

混沌模式，网页可以创建多个相同的键，一样的键不同的位置 N宏

高级压枪
全局压枪板块可以绑定热更新键 绑定后弹出展开输入 备注 速度 （多此一举）
可输入动态速度流 如只有50就为50 ，如果为50 50 25 20 50 10 ，识别为压枪速率空格持续时长，空格到下一个，压枪速率空格持续时长，……（熊猫）N
可根据键映射的触点决定压枪，（转为触点识别），如连点连发就在左键映射触点后才压枪，释放触点不压。
压枪速度样式可加减按钮加0.5减0.5。N
压枪速度微调键，比如以当前速度加0.5或键0.5。N
高级压枪可以弄个曲线编辑器，点与点的曲线链接，每一小段调整压枪速度间隔，，UI交互难整
可以添加触发组合键，用于蹲下或趴下的后坐力压枪。可以同步按住，可以开关
https://github.com/RiderLty/input2com_linux 做压枪前先理解这个项目，触摸板传输……


组合键可以在按下时不再让单键映射的模式开关，仅作用于按下的组合键，比如Z+X，Z，X，按下Z触发Z，再按Z+X 后不再触发Z和X的单键，只映射Z+X组合键，不会影响其他单键，叫什么拦截模式，迸发模式，沉浸模式，对单键功能拦截，作用范围包括各个功能:宏，压枪，映射开关切换 
这里组合键优化可以参考熊猫:如绑定Z+X，先按Z触发Z单键映射，再按X不触发X单键，而是触发Z+X组合键，


 按键触点动态偏移。（长按不再持续为一个位置） Y
 可以开启按键仍然可以在关闭映射状态下映射，不强制释放，
 
可单独调节按钮的随机落点半径，并可视化。更好的可视化。交互逻辑为开启开关，开启上面的开关，下面的已添加键位列表会出现滑块调整单独调整，点击类是随机落点，视角类就是视角随机重置。宏也要加。

更好的可视化，如半透明键位显示 Y， 更好的字体显示，
前端可视化:滑动键类型的可视化 错误跟随。把滑动的可视化半径调成固定且不受其他参数干扰
前端的轮询点击逻辑有些不好，改成pit显示第0个触点。到下次更新keys.js再解决。解决前端keys.js面板单键组合键 使用 轮询点击 显示交互有些不好，虽然背景图片映射位置有一个点，但是在pt子卡片列表里没有这个位置，让pt显示第0个触点？或者把轮询点击的 添加触点 按钮改名为 添加轮询位置 
前端 按键配置板 块新模式:完整显示键名 开关 ，开完整显示如KEY_Z 关闭只显示 Z， 鼠标键省略显示ban，手柄键也省略 ……

前端可以导出导入配置文件（整个）
前端可以导入导出单个板块面板功能字段的文件，如宏。（单独）

前端点一下可视化位置会自动让设置窗口滑动跳到这个按钮或者板块的位置（会自己展开），并呼吸闪烁以提示

背包键，第二次，点击B后，延迟几毫秒后才切出指针
滑动的键功能，加可设置滑动速度，贝塞尔曲线路径。

混合映射模式:比如轮盘用uinput，视角触点用inputmanager。 N


宏录制映射触点，，网页上可播放路径，也就是录制宏，一个可以开始开关录制N
宏:网页上可以绘制录制滑动（复杂曲线）路径……v99
宏模块可以选择键映射模板，控制轮盘模板，控制视角模板，录制模板（有一个键录制，然后可以调速度）v99

宏v2:触发条件添加 指针切出时 未切出指针时，事件添加 切出指针 关闭指针，模板添加 轮询模板 可以添加轮询次数。
轮询模板似乎可以套 前面三个模板，比如按抬模板轮询版……
简单的轮询模板只能按下触发不能抬起，    决定做按抬轮询模板 ，但交互规则……  把所有模板底部加一个创建轮询按钮？  
宏加一个事件:同步按住抬起，逻辑为执行到就按下一个位置 抬起当前绑定就释放触点，如果事件位置已存在触点触发就不再执行，必须检测到当前绑定键处于按住按下时才能触发，否则跳过
宏事件加个 开启切出指针，关闭切出指针 仅可在开启映射状态下执行
宏与配置管理器互通v2
后端宏管理器:当一个键绑定多个模板时只能有一个模板生效，只有最新添加的那一个绑定键模板生效
可以加一个开关，同一绑定按键全触发模板并发
加一个开关:拦截其他键功能，只触发宏
前端宏编辑器 的事件内容 加个窗口容器  固定长度


把左键（开火）同步按下抬起键切换成连发键 或连发键切换成同步按下抬起键 的快捷，按键绑定可以单独记录设置连发的ms （其实把侧键设为连发就行），或者连发必须组合键触发 N
也就是高级连发单独板块，触发条件为左右键触发，然后不触发时会执行左键的键功能映射，触发不会映射键功能样式的触点。要可以 放置三个位置 左键同步按抬位置 右键键同步按抬位置 连发组合键位置 


-v鼠标 的右键按下长按为快速两次点击后间隔xxms后继续两次点击，或者快速点击可设置间隔  。中键点击为瞬移到复位位置，侧键前进为调高鼠标速度点击加0.5，侧键后退为调高鼠标速度点击减0.5，侧键前进位左滑，侧键后退为右滑；
右键可以映射滑动，配置路径，逻辑是以鼠标位置为起点映射滑动到目标位置，仍然范围内随机  Y
-v悬浮指针可以被手柄右摇杆控制移动，可以定义手柄的键是什么作用，仍然可以自定义滑动宏。
原版在启用-v关闭映射时，手柄RT键就等于鼠标左键，我想让这个键可以自定义，或者直接掐掉，把这个硬编码给移除
-v 悬浮鼠标 指针切出时 拦截键盘上下左右键（包括手柄），按下方向键会映射相同方向的移动触点（与滚轮参数相同）开启反转滚轮时就反转，加个开关？

为了降低占用，可以有一个模式启动参数来禁用前端编辑，不启用前端广播 N

解决前端拖动设置窗口会穿透的问题，导致整体页面滚动 N 全屏解

调整窗口大小前端的设置窗口常规设置 加 按钮可以把设置窗口放大25％或减小25%，点击这两个按钮以改变，初始为原来的尺寸 重新载入前端回复，不会写在配置页面。 Y


一个debug可以滑动鼠标刷屏查看远程控制延迟，


程序也可以做个启动参数，用来限制映射触点的最终采样率输出hz，不不不，可以仅 限制 视角触控的最终触控率，可以在视角设置里调参数。 Y加一个限制视角控制的最高采样率限制，必须能开关，防止意外，  Y

防止键粘黏的更细处理，这里用指针切出解决 N


MOBA:不 启用wasd轮盘开关
最终———————————最终———————————

agtmrcp限制采样率是限制鼠标的上传频率……
手机回报率解限制专用的app，做USB驱动连接gtm远程模式，无adb可用，可达到1000hz以上（仅xy的回报率，鼠标按键不限制）。然后可以不息屏，ui界面为高版本安卓风格样式，主题为深色。蓝牙连接的鼠标等办法。

重构虚拟光标app，把图像换成归一化绘制的 矢量图（不是图片不是固定像素）更清晰的光标。悬浮鼠标app改逻辑隐藏痕迹防图层检测或录屏，具体不知该如何操作 ；加显示IP端口 和 权限获取状态，Y

修复重置 inputmanager.apk 的压力值和 面积模拟 https://github.com/RiderLty/inputManager-touch-interface 用原版scrcpy对比更新；并且使用本机共享内存通信，而不是uds ，追求更低的延迟 Y



做一个 安卓蓝牙模拟管理器，gtm程序可以通过 安卓蓝牙管理器 来控制App发送控制信号，测试。报文找AI拿资料。

做一个gtm启动器app，什么星际信标启动器，可以远程dpu，，前端有什么就什么，一个单纯的启动器，可以勾选启动参数添加，可以自定义启动某个包名图像的启动参数启动文件？。

给AI优化视角移动 ，hanlder优化代码，降低延迟，和修复神秘抬头 YYNN，这是最后要试试


参考——————————————
参考其他家映射的细节

未映射:)鼠标滚轮滑动的由慢至快的滑动速度，结束滚动时就会慢下来滑动一小段距离（延迟）（惯性）（k2er）

智能压枪:智能识别，因素来计算倍率，可视化动态压枪流，可用组合快捷键快速调节速率，……   （佳映、，N指像素压枪，），理论上gtm甚至可以搞AI识图压枪，甚至AI自瞄不开玩笑，#我做不到，gtm后人可能会有大牛巨佬做出来。

视角设置可以把半径边界设为碰到归中或停下（不归中），  （k2）
小眼睛可单独设置速度，半径（跟随），各种延迟，与视角设置一致（k2）
可以自定义使用启用外设，如关闭勾选或单独使用键盘或鼠标（k2）

——————————————
v x.x.x——————————
逻辑问题————————————————
网页的鼠标键不能识别绑定鼠标键，要调成只不能识别左右键和上下滚轮就行，中键侧键可识别。 XXX 别整

程序的inputmanager模式压力异常，显示为65535，正常情况下是1，

先专攻视角修复，怀疑视角重置时出现的问题 是或者v悬浮鼠标的切出位置导致跳视角。严重怀疑是安卓的触控处理，采样率过高导致系统承受不住压力，导致错误。无法修复，因为任何的映射都有这种情况。通过ai了解是安卓触控内核的算法有些问题，防抖边缘防误触防手掌等等参数导致的。无解，就算熊猫和其他映射也有这个问题。复现描述: 缓慢移动视角触点，并有另外两个触点高频重复点击，每次重置释放视角触点再重新映射都会高概率触发跳视角，可以缓解:降低重置归中次数，把速度调高
——————————————————————
存疑——————————————————————
游戏是否可以监听程序的远程网络数据传输，如-r的控制数据，-v的控制数据。

——————————————————————————————————————
